import SpellDataModel from "../../../../model/item/character/SpellDataModel"

export const SpellReactComponent = ({ item }: { item: Item & { system: SpellDataModel } }) => {
    return (
        <div>

        </div>
    )
}